"use client"

import { 
  motion, 
  AnimatePresence, 
  useScroll, 
  useTransform, 
  useSpring, 
  useAnimate,
  useInView as useFramerInView,
  useAnimation
} from 'framer-motion'

// Re-export framer-motion components
export { 
  motion, 
  AnimatePresence, 
  useScroll, 
  useTransform, 
  useSpring, 
  useAnimate,
  useFramerInView,
  useAnimation 
}